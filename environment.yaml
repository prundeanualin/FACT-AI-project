name: fairlisa
channels:
  - defaults
  - pytorch
  - conda-forge
dependencies:
  - python=3.9
  - pip=22.2.2
  - pip:
      - torch
      - pandas
      - numpy
      - scikit-learn
      - tqdm
      - openpyxl